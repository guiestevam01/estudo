Introduzido no JDK 5.0;
no artigo diz: "reduzir erros e adicionar uma camada extra de abstração sobre os tipos" 
  - O que ele quis dizer com camada extra de abstracao sobre os tipos? Eu tenho um tipo de dado
    entao seria eu adicionar mais uma camada sobre isso?
Eu tenho um atributo de uma classe, em alguns caso ele pode ser inteiro, outros float, outros String e assim por diante.
Posso usar 
Object x;
Mas fazer isso significa fazer casting de tipos. Isso e perfeitamente possivel claro porque tudo herda de Objects
tipo, objects tem metodos de instancia:
"protected Object	clone()	
Creates and returns a copy of this object.
boolean	equals(Object obj)	
Indicates whether some other object is "equal to" this one.
protected void	finalize()	
Called by the garbage collector on an object when garbage collection determines that there are no more references to the object.
Class<?>	getClass()	
Returns the runtime class of this Object.
int	hashCode()	
Returns a hash code value for the object.
void	notify()	
Wakes up a single thread that is waiting on this object's monitor.
void	notifyAll()	
Wakes up all threads that are waiting on this object's monitor.
String	toString()	
Returns a string representation of the object.
void	wait()	
Causes the current thread to wait until another thread invokes the notify() method or the notifyAll() method for this object.
void	wait(long timeout)	
Causes the current thread to wait until either another thread invokes the notify() method or the notifyAll() method for this object, or a specified amount of time has elapsed.
void	wait(long timeout, int nanos)	
Causes the current thread to wait until another thread invokes the notify() method or the notifyAll() method for this object, or some other thread interrupts the current thread, or a certain amount of real time has elapsed.
"
entao supondo que p1 seja uma instancia da classe P1 posso fazer claramente p1.toString(); porque ele ja herda Objects.
Em java posso qualquer objeto pode ser convertido para Object.
String s = "oi";
Object o = s; // upcasting implícito    
Agora converter Object para uma classe especifica tipo. 
Enfim basicamente temos classe e um atributo que nao sei exatamente o tipo de dado que ele pode ser e ao inves de eu implementar ele como Object e fazer toda aquela coisa de conversao chata que da erro em tempo de execucao posso fazer:
public class Foo<T>{
  T x;
  
  public Foo(){}
  public Foo(T x){
    this.x = x;
  }

  public void printX(){
    
  }
}
Entao o T indica que essa classe vai ter um tipo especifico no momento da instancia, no caso posso tratar de membros. COmo eu disse, seria uma classe generica no momento da instancia, nao trabalho com generico em estaticos, isso e meio obvio.

public class Example2 {
    public static void main(String[] args){
      Foo<String> f1 = new Foo<String>();
      f1.x = "Hello";
    }
}
